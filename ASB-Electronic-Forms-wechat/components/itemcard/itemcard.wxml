<!--pages/navtab/navtab.wxml-->
<view class = "inputBoxWrapper" id="{{name}}">
  <text class = "smallText"> {{name}} ({{value}} pt)</text>
  <text class = "smallText"> {{quantity}}</text>
  <image class="itemImage" src="{{imgurl}}"></image>
</view>